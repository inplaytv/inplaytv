-- Show ALL competition_entries (no filters)
SELECT * FROM competition_entries ORDER BY created_at DESC;

-- Show ALL competition_instances (no filters)
SELECT * FROM competition_instances ORDER BY created_at DESC;
